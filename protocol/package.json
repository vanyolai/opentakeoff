{
  "name": "opentakeoff-protocol-checks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Development-only validation of the draft Takeoff Protocol; not a runtime package.",
  "scripts": {
    "preflight": "node scripts/preflight.mjs",
    "adapt": "node scripts/adapt.mjs",
    "test": "node --import tsx --test test/*.test.mjs",
    "check": "npm test && node scripts/check-docs.mjs && node scripts/check-matrix.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "tsx": "^4.19.2"
  }
}
