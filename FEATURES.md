# FEATURES.md — every capability, mapped to its code

The buildable map: what OpenTakeoff does and exactly where each piece lives, so you (or your coding agent) can extend a specific capability without spelunking. The UI for nearly everything is in `web/src/pages/TakeoffCanvas.jsx` (one deliberately monolithic component); the pure logic lives in `web/src/lib/`.

| Capability | What it does | Where the logic lives |
|---|---|---|
| **Ingest** | PDF, image, or `.zip` plan set—unpacked and normalized in-browser, multi-page, up to 4 sheets side-by-side | `web/src/lib/ingest.js`, sheet layout in `TakeoffCanvas.jsx` (`panels`, `panelAt`) |
| **Rendering** | pdf.js raster per sheet + **crisp detail-view**: past ~1.15× zoom the visible region re-renders from vectors at current zoom | render chain + detail-view effect in `TakeoffCanvas.jsx`; pdf.js (`pdfjs-dist`) |
| **Scale** | Auto-detect the drawn scale note per sheet; calibrate from a known dimension; per-sheet memory | `detectScale` in `web/src/lib/sheets.ts`; calibrate flow + `uppFor` in `TakeoffCanvas.jsx` |
| **Scale verification** | **Check a dimension (K)**: measure a printed dimension read-only, compare with what the drawing says (% error verdict, one-tap recalibrate); accepting any scale drops an ephemeral calibrated **ruler guide bar** on the sheet so a 2×-off scale is obvious before tracing | check flow + `showScaleGuide` in `TakeoffCanvas.jsx`; `ftIn`/`fmtCheckLen`/`parseLenInput` in `web/src/lib/units.ts` |
| **One-Click Area** | **Temporarily gated** (off the rail, `O` reports it, `VITE_ONE_CLICK=1` lifts it — `docs/design/ONE_CLICK_GATE.md`) while the flood engine is re-validated against a wider plan corpus; the engine and its bench are unchanged. When on: click inside a room → flood-fill against PDF linework → traced polygon, vertices snapped to true corners. **Gap-sealing (RFC #60 item B, contributed by Kevin Murphy (@knmurphy)):** a fill that leaks retries through a scale-aware seal ladder (distance-transform dilation, never-ascending growback, room-size + real-boundary gates) so drawn openings bound rooms honestly, every seal on provenance (`origin.gap_sealed_px`). **Door wedges:** doorways closed by drawn swings—bezier or CAD-tessellated polyline arcs—measure through the door to the wall opening (`origin.door_wedges`; policy: swing included, to the wall plane). **Minimum-passage rule:** a drawn gap under half a foot never connects two spaces, at any resolution; feet-true thresholds ride `MaskObj.mppf` and the mask is pinned to the page in points, so render scale can't move a measurement. **Hatch-robust:** per-stroke lattice periodicity classifies hatch/poché (finish zones separate—a patterned floor measures as its own click), transparent to an escalated pass. **Confidence:** every trace carries a 0–1 `origin.confidence` with named factors. **Scan-capable:** sheets with no vector linework build the boundary mask from rendered pixels instead (adaptive threshold + gap-bridging closing) and run the same sealed flood/trace—results badged and stamped `raster_traced` | `web/src/lib/oneclick.ts` (`extractVectorGeometry` + per-segment meta, `classifyHatchSegs`, `markPolylineArcs`, `buildMask`, `floodRegionSealed`, `oneClickRing`); confidence in `web/src/lib/confidence.ts`; scan mask in `web/src/lib/rastermask.ts` (`buildRasterMask`, `rasterMaskScale`), trigger policy + `ensureRasterMask` in `TakeoffCanvas.jsx` |
| **Accuracy benchmark** | The RFC #60 instrument (contributed by Kevin Murphy (@knmurphy)): `npm run bench`—part of `check`, so no engine change lands without corpus results—scores synthetic truth-by-construction fixtures plus pinned, adjudicated traces of the bundled demo plans by rasterized IoU (mean/floor, refusal/leak/correct-refusal rates, confidence gate, cross-resolution determinism gates). `bench:callouts` cross-checks against the plan's own printed `NNN SF` annotations (reports, never gates); `bench:batch` scoreboards the batch detector; `pin-goldens` enforces the adjudicated re-pin protocol | `web/bench/` (`run.mts`, `score.ts`, `corpus.ts`, `corpus/*.json`, `callouts.{ts,mts}`, `batch.{ts,mts}`, `pin-goldens.mts`) |
| **Manual measure kit** | Area (**⌥-click a point to curve the boundary through it**—straight walls and bowed ones in one trace, flattened into a single closed ring at commit so SF/LF/Cut Out/exports see an ordinary polygon), Rectangle, Linear, **Curved Line** (smooth spline through clicked points—LF at the true curved length), Surface-Area (walls), Count, Eraser (deduct math), **Flip Horizontal/Vertical** (mirror the selected shape about its own center—an isometry, SF/LF unchanged). Shapes stack by role tier—fills at the bottom, runs above, count pins on top—and selection scans the same stack top-down, so an Area drawn over a Counter/Line/Surface never traps it (#116). **Edge-insert ghost (#237):** hover a selected shape's edge and a `+` ghost rides it under the cursor (clear of the corner/mid-edge grips); pressing inserts a vertex at that exact spot and drags it out in one gesture—no modifier | tool state machine in `TakeoffCanvas.jsx` (`performClick`, `finishShape`, `commitPoly`/`commitLinear`/`commitSurface`, `flipSelected`, `edgeInsertHitAt`); stacking in `ROLE_TIER`/`stackedShapes`, `TakeoffCanvas.jsx`; mirror math in `reflectVertsNorm`, `web/src/lib/geometry.js`; curve math in `web/src/lib/curve.js` (`flattenCurve`, `flattenRing`, `flattenClosedCurve`) |
| **45°/90° angle lock + aim cursor** | OS pointer hides on canvas; hairlines + house star are the cursor; in-progress segments lock to the 45° family (4° tolerance, ⇧ = hard lock) and the click commits the on-axis point; star swells, band thickens, chip reads angle + live length | `angleSnap()` in `web/src/lib/geometry.js`; the lock block in `moveCrosshair`, `TakeoffCanvas.jsx` |
| **Endpoint snap** | Cursor snaps to true PDF endpoints (spatial hash of extracted vectors) | `buildSnapGrid`/`nearestSnap` in `web/src/lib/geometry.js`; endpoints from `oneclick.ts` |
| **Conditions and count objects** | User-defined condition types: color + CAD hatch, waste %, ×N multiplier, wall height, border thickness; Count objects keep the original color marker or use a built-in outlet/switch/light/data/camera symbol with no/tag/custom/sequential plan labels. Sequential prefixes issue stable per-instance identifiers editable after placement. The additive model already resolves a condition-level authored `layer_id` and a presence-aware object override; layer UI follows later | presentation/layer model in `web/src/lib/objectPresentation.js`; SVG marker in `web/src/components/ObjectMarker.jsx`; editor + picker in `web/src/components/TakeoffsPanel.jsx`; placement/render/instance-label edit in `web/src/pages/TakeoffCanvas.jsx`; print path in `web/src/lib/markedset.js`; totals math in `web/src/lib/totals.js` |
| **Supporting Materials** | Per-condition labor type + subfloor type (free text) alongside supporting materials: coverage rate + basis (floor SF or m² / linear LF or m / each / **figured seam length**, the roll layout's own weld-rod length) → order qty rounded up; inputs, presets, CSV, and Excel follow the active unit system without rewriting stored rates; per-material coverage presets (adhesive/mortar trowel notches) + a grout calculator that derives SF/bag from tile geometry | `MaterialsEditor` + labor/subfloor inputs in `web/src/components/TakeoffsPanel.jsx`; display conversion in `web/src/lib/coverageUnits.js`; presets + grout math in `web/src/lib/coverage.js`; qty and CSV logic in `web/src/lib/totals.js`; workbook export in `web/src/lib/xlsx.js` |
| **Condition twins** | The same finish measured in another area, with its own supporting materials: duplicating a condition names the variant (`SV-1` → `SV-1 – Level 2`) and the copy keeps **following** the original—change a rate on the family and every twin that hasn't touched that row gets it, edit a row on the twin and only that row goes local (`✎`, revertable). A removed row leaves a struck-through tombstone a later family edit can't undo; **Split out** freezes the following rows and ends the inheritance. Propagation is on write, so every reader still sees a fully populated materials list | `web/src/lib/variants.ts` (the whole rule, pure); the family seam + duplicate/split in `TakeoffCanvas.jsx`; row markers, tombstones and the inline duplicate input in `web/src/components/TakeoffsPanel.jsx`; `web/test/variants.test.ts` |
| **Transitions (canvas)** | **⟂ Transitions…** in the Takeoffs panel: with the transition condition active, pick the two finishes that meet and derive the runs between their committed rooms—the estimator-side half of `derive_transitions` (#202). Flood-traced rooms do NOT share edges, so what is there is proximity in two flavors: a **butt joint** (median gap within an inch—one open space, no wall) commits as a dashed linear shape on the active condition through one `add` command (one ⌘Z for the sweep), stamped `origin.derived` with both parent shape ids, both tags, `case: "butt"` and the measured gap, and inked by the existing Accept pill; a **wall-separated** run is adjacency across a partition, whose transition is a threshold in a doorway the trace record cannot locate, so it is listed under *Reported, never counted* with its length, the wall thickness, and a **look** link that centers the sheet on it. Refuses the whole call (nothing partial) when the transition would land on one of the finishes it separates, when a sheet in play has no scale, or when either finish has no rooms on the open sheets | `deriveTransitionRuns` / `transitionRefusal` / `dropCollinear` in `web/src/lib/transitions.ts` (`sharedRuns` is the shared geometry, also imported by the MCP verb); `TransitionsAction` in `web/src/components/TakeoffsPanel.jsx`; `deriveTransitionsOnto` + `locateSheetPoint` in `TakeoffCanvas.jsx` |
| **Roll goods** | Opt-in per condition (`roll_setup`: material class, roll width, max roll length, seam/wall allowances, direction, sell unit) → the engine figures lanes, seams, multi-roll splits, and order footage; cuts draw to scale over their rooms in material-true colors, numbered in cutting order; edit mode slides/resizes cuts (undoable through the `rollcut` command, double-click resets); the docked Roll panel shows cuts nested ON the roll with dimensions and drag-to-reorder re-packing; Roll Order LF + Rolls + **Seam LF** (the figured weld-rod/seam-tape length—adjacent lanes of the SAME room, net of wall overage, only where the lanes overlap; never across the wall between two rooms) ride the Report/CSV/XLSX and `report.v1`, and a materials row on the `seam_lf` basis divides against it | engine `web/src/lib/rollgoods.js` (pure—Hartman's Summit port); bridge `web/src/lib/rollTakeoff.js`; overlay + drag + memo in `TakeoffCanvas.jsx`; `web/src/components/RollPanel.jsx`; setup UI in `TakeoffsPanel.jsx`; report columns `rollColProfile` in `web/src/lib/reportColumns.js` |
| **Sheet graph** | The plan-set index for agents: sheet roles with evidence, schedule tables extracted with a citation bbox per cell, room tags with names, detail callouts, and room-tag resolution (tag → room-finish row → finish definitions, every edge cited; unresolved comes back with a reason). Reads the real-set shapes: a schedule continued across sheets ("— CONT'D") is ONE table whose rows cite the sheet carrying them; rotated (quarter-turn) column headers anchor tables and are flagged; multi-building sets key rooms by (building, number)—an unqualified reused number refuses listing the candidates per building, a qualified tag ("A-134") picks the building the set names; revision markers—text ("Δ2" / "REV 2") and drawn deltas (a bare digit inside a digit-scale triangle of linework, proven from vector geometry and flagged drawn)—attach to the schedule row or room bubble they sit beside and ride `resolve_tag` as `revisions`—the codes are the post-revision answer and the consumer is told the ink changed (a marker never bands into a cell or mints a room key; a revision cloud with no text marker is linework, stated as invisible to the text-layer pass); wide REMARKS columns band precisely—wrapped/baseline-offset remark lines merge into their own row within the table's row pitch, cited, and a notes block below the table stays out; two-tier headers (a merged WALLS parent over N/E/S/W sub-columns) anchor each sub-column under its parent with real bounds, so a left-aligned wall code never lands in the narrow BASE column; a neighboring legend can't bleed into the last column; and a DOOR/WINDOW/PARTITION schedule is refused as a finish table by title (a finish code must never chain to a door), the drop named in notes. Columns are read from where the DATA starts (headers are centered, cells are left-aligned) and three-tier headers name duplicate columns by their parent (FLOOR FINISH vs CEILING FINISH); numbers are CORROBORATED before being called rooms, and keynote/legend numbers go to `unmatched_tags` with a reason rather than being counted as rooms that the schedule forgot. Scored against real bid sets by `mcp/scripts/graph-eval.mjs` on cell accuracy AND tag classification—1.000/1.000 on both across three contractors. MCP tools `sheet_graph` / `resolve_tag` / `find_schedule`—and `detect_rooms assign_from_schedule` drives the whole chain per room: each detected room commits under the FLOOR finish its own row states, rooms the schedule can't answer for are withheld with reasons and re-seedable coordinates, never committed under a guess | `web/src/lib/sheetgraph.ts` (pure); MCP wiring in `mcp/src/session.ts` + `mcp/src/tools.ts` Schedules of every family index: room-finish, finish/material, and equipment — the device tables of any trade (fans, pumps, heaters, light and plumbing fixtures, diffusers), keyed by mark and proven by a device column, every stacked schedule on a sheet read top to bottom. |
| **PDF layers (OCG)** | CAD-exported sheets state what their ink is—the engine reads it: per-segment layer attribution from marked-content operators, layer names classified into roles (boundary / finish-pattern / annotation / structure / demolition), and the flood mask consuming the stated roles ahead of the hatch heuristics (hidden layers excluded outright); unlayered sheets are byte-identical to before. On the canvas, a docked **Layers** panel lists the sheet's table (names, stated roles, default visibility) with per-layer Auto / Wall / Off controls feeding the same mask seam—overrides persist per sheet, and the control is invisible on sheets without layers. The lift is scored, not asserted: a corpus IoU eval gates it in CI (role-filtered vs roleless masks against stated ground truth; zero delta on unlayered scenes) | marked-content stack in `web/src/lib/oneclick.ts` (`extractVectorGeometry`), roles + overrides in `web/src/lib/layers.ts`, mask short-circuit in `buildMask`; panel in `web/src/components/LayerPanel.jsx`; corpus eval in `web/test/layerIoU.test.ts`; MCP surface (`sheet_info.layers`, `layers` filters, `layer_bounded` provenance) in `mcp/src/session.ts` |
| **Assignment provenance** | Where a shape's finish tag came from, on the record: `origin.assignment`—`{source: "schedule", room_tag, surface, schedule_sheet}` with the citation when a schedule row decided, `{source: "asserted"}` when the agent chose. Stamped centrally at commit so no agent path ships unstamped; a reassigning edit flips schedule → asserted; `list_shapes` carries the flat verdict and the marked-set cover discloses the split, with a staleness drop so it never calls a room withheld after it was committed by hand | `mcp/src/session.ts` (`commit`, `floorTagFor`), disclosure in `mcp/src/marked.ts` (`assignmentDisclosure`), cover line in `web/src/lib/markedset.js` |
| **Totals and report** | Per-condition Floor/Wall/Border SF, LF, EA, SY, with/without waste (waste-inflated columns labeled "w/Waste") + materials buy list; a **Labor view** column preset surfaces raw Total SF and hides the waste columns for tying quantities to labor externally | `web/src/lib/totals.js`; `web/src/components/ReportPanel.jsx` |
| **Export** | CSV / JSON / **Excel (.xlsx)** / print / **Marked Set PDF**—the workbook carries Summary, By-sheet, Materials, Shapes-audit, and **By floor × room** tabs (the sheet × label cross-section, ordered quantities per cell, unlabeled work rolled up per floor so a floor's rooms reconcile) (own SpreadsheetML writer, formula-inert strings, full-precision cells); the marked set burns the takeoff into the sheets (colors, clipped hatch, quantity chips, markups) + a legend cover with net totals and a by-sheet breakdown; follows the current light/dark view | export handlers in `ReportPanel.jsx`; `web/src/lib/xlsx.js` (+ `sheetTotals` in `totals.js`); `web/src/lib/markedset.js` |
| **Revisions** | Save the takeoff as a named revision at each bid revision/addendum; compare any two (or one against the live takeoff) as quantity deltas per condition, per sheet, and on the buy list, with a compare CSV; restore auto-banks the live takeoff first | `web/src/lib/revisions.js` (diff), `web/src/components/RevisionsPanel.jsx`; `revisions` store in `web/src/lib/store.js` |
| **Markups** | Freehand highlighter (chisel/round nib, 5 inks, F/M/B sizes, style remembered), revision clouds, callouts, text notes—separate layer, never counted—plus the **dimension line** (two endpoints, end ticks, labeled with the measured length at the sheet's scale—a standalone answer when the plan prints no dimension; drawn two-click in the canvas (`N`, live length chip) or placed through MCP `annotate`, scale-gated either way, rendered on canvas and in the marked set) | markup tools in `TakeoffCanvas.jsx` (`placeMarkup` + the highlighter pointer branches); `cloudPath` + `thinStroke`/`strokePathD`/`chiselRibbon` in `web/src/lib/geometry.js`; highlight + dimension export in `web/src/lib/markedset.js`; `dimLabel` in `web/src/lib/units.ts` |
| **Approval stamps** | Ink over pencil: the human-only **Approve** tool seals a committed shape (the record carries its `shape_id`) or a sheet point with a circular APPROVED ring; agent verdicts are the same record family (`actor: "agent"`) rendered as an AGENT diamond—minted over MCP by `mark_verdict` (shape-anchored or sheet-point, optional note, one mark per shape) and lifted by `delete_verdict`, which refuses the estimator's seal; neither takes an actor input, so the human ink stays structurally out of agent reach. Placing or lifting a seal is a real `⌘Z` step (family-tagged entries on the shared undo stack) and an exact `undo_last` step server-side; seals burn into the Marked Set above the markups and the cover gains an *estimator-approved · agent-marked* tally line; the family rides `export_takeoff`/`import_takeoff` and the app's Sheet-menu import (append-new-ids, skip-same, `sanitizeApprovals`-gated) | pure command layer + load gate + tally + token inks in `web/src/lib/approvals.js`; tool + seal layer in `TakeoffCanvas.jsx` (`placeApproval`, `dispatchApproval`); PDF seals + cover line in `web/src/lib/markedset.js`; MCP verbs in `mcp/src/{session,tools}.ts`; tests in `web/test/approvals.test.ts`, `mcp/test/tools.test.ts` |
| **Symbol sweep** | Every instance of a repeated plan symbol from ONE example: fingerprint the segments inside a seed marquee, propose placements by constellation anchoring (rare-length anchors—no naive scan of 100k-segment sheets), score as the length-weighted fraction of seed linework reproduced within tolerance, under 0/90/180/270 rotation + mirroring. ≥ 0.92 matches; 0.75–0.92 is withheld with reasons; the work cap is disclosed, never silent. **Set-wide** (`scope: "set"`): sweep the whole working set counting on PLAN-role sheets only (sheet-graph roles; every exclusion disclosed with reason, per-sheet caps + wall-clock)—seed from a detail/legend sheet and it serves as fingerprint source while never counting itself. **Schedule-row seeding** (`sweep_schedule_row`): mint the condition from the row itself, anchor the fingerprint on the tag's drawn marker (pad ladder + corroboration at a second occurrence), count only where geometry AND the row's own tag text agree—sibling-tagged markers excluded by name, unlabeled ones withheld, unanchorable rows refused (a fingerprint is never guessed from text alone). **Counter-examples** (`exclude`, #259): rects around instances you do NOT mean, marqueed exactly like the seed—the rect's own contents choose the mechanic, never the caller (extra contained linework fingerprints like a positive; a rect with nothing extra reads as the line running THROUGH it, which a real instance drawn over it breaks—inexpressible as a seed, since only fully-inside segments define a symbol), tested under the seed's own symmetry group so a rotated/mirrored copy of the excluded thing doesn't come back counted; every rejection disclosed in `rejected[]` (which negative, its evidence fraction, a reinstatable placement), a negative that separates nothing refused with instructions. **Stroke-luminance gate** (`luminance_tolerance`, #260): for flattened exports where the layer tree and pen weights are stripped but the file still states stroke color—`VectorGeometry` carries per-segment Rec. 709 luminance (`lum`, graphics-state-correct through save/restore and Form XObjects), and a STATED tolerance holds candidate segments to the seed's own pen; opt-in both ways (no channel or no tolerance = pre-#260 scoring, bit for bit), and `lum_gate` disclosure names every placement the geometry would have committed that the pen pulled under the bar. **Labels** (#308): for labeled families the sweep reads the drawing's own names — a fixture token written beside a placement (radius scaled off the token's text height) or connected by a drawn leader (chased along the annotation pen, armed only on multi-pen sheets) — as `label`/`label_via` on every row plus `seed.label`; the reply flags shape-only matches in a labeled family, withheld rows the drawing vouches for, and matches the drawing names differently. Disclosure, never a recount; resolution is pure in `web/src/lib/symbollabels.ts`, shared with the canvas Symbol tool (#264). **The canvas Symbol tool** (#264): the same engine by hand — rail tool (Y), two-click marquee around one instance, matches/questions/seed reviewed with translucent glyphs and label lines, keyboard walk (↵/X/→), floor-not-total banner, commit as EA counts in one undo step with the seed in by default; `TakeoffCanvas.jsx` + the shared pure libs, so canvas and MCP cannot disagree. MCP commits match centers as EA count markers (one undo step for the whole sweep, `origin.method "symbol_sweep"` with per-marker score/transform + seed source, schedule rows with `origin.assignment {source: "schedule"}` and the row citation) | `web/src/lib/symbolsweep.ts` (pure: `fingerprintSymbol`/`matchSymbol`/`sweepSymbols`); MCP wiring in `mcp/src/session.ts` (`symbolSweep`, `sweepScheduleRow`) + `mcp/src/tools.ts`; fixtures `mcp/test/fixtures/symbol-plan.pdf` + `symbol-set.pdf` (`mcp/scripts/make-symbol-fixture.mjs`) |
| **Stitched sheets** | A floor split at a match line joins into ONE working surface: select 2–4 sheets in the gallery → **Stitch** → they butt edge-to-edge and open as one panel with its own key (`stitch:<uid>`), so a room crossing the match line traces as one shape and every tool—measure kit, count, markups, calibrate, One-Click (merged snap grid + flood mask)—works unchanged on the composite. **Align** joins the match line with two clicks (the Calibrate idiom; overlap splits at its midline behind seam-clip wrappers). Persisted additively (`stitches`, sanitize-gated); delete refused while takeoffs live on it; cross-panel traces in a plain side-by-side group refuse at commit and point here | `web/src/lib/stitches.ts` (pure: sanitize, extent, auto-butt, align solve, seam clips, geometry merge); canvas wiring (`createStitch`/`openStitch`/`stitchAlignAt`, `drawPanels`, render-effect stitch branch) in `TakeoffCanvas.jsx`; gallery button + strip in `web/src/components/PlanNavigator.jsx`; tests in `web/test/stitches.test.ts` |
| **Sheet revisions** | A re-dropped file whose bytes differ never silently overwrites: `addPdf` content-hashes (SHA-256), archives the old bytes as a numbered revision atomically with the swap, and the canvas surfaces **"sheet changed under your markups"** + re-renders the new paper (identical bytes = no-op; image wraps are byte-deterministic so re-drops can't false-alarm). `listPdfRevisions`/`loadPdfRevisionData` are the read path the slip-sheet overlay and diff-driven re-review build on | `web/src/lib/store.js` (`addPdf`, `pdf_revs` store, `listPdfRevisions`, `loadPdfRevisionData`); revision toast + doc-cache invalidation (`docEpoch`) in `TakeoffCanvas.jsx`; `updateMetadata:false` in `web/src/lib/ingest.js` |
| **Dark view (negative print)** | ☾ in the zoom cluster inverts sheet pixels in place (one difference pass, an involution—no filter layers, no re-render) with dark hatch/fill variants baked into the patterns | `invertCanvasPixels` + dark pattern ids in `TakeoffCanvas.jsx` |
| **Persistence** | Autosave to IndexedDB + localStorage; survives reload; nothing uploaded. **Export takeoff… / Import takeoff…** (Sheet menu) round-trip the whole editable takeoff through a `takeoff_canvas.v1` JSON file — the same document autosave writes and the MCP `export_takeoff` emits — so a project can be backed up, archived, moved between machines, or handed to another estimator | `web/src/lib/store.js`; `exportTakeoffFile` / `importTakeoffFile` in `TakeoffCanvas.jsx`; merge rules in `web/src/lib/importTakeoff.js` |
| **Project archive (.otk)** | The whole job as ONE portable file (#300): **Sheet → Export project archive…** zips a versioned manifest (`opentakeoff.project.json` wrapping the *exact* autosave payload) with every stored plan's bytes verbatim (`plans/`, level 0 — PDF streams are already deflated). Drop a .otk (or Add plans) and the workspace becomes the archived project: current non-empty takeoff snapshotted first, name-collision plans archived as revisions (CO-1) — never a silent overwrite; unknown archive schemas refuse loudly instead of half-loading | `web/src/lib/projectArchive.js`; `exportProjectArchive` / `importProjectArchive` + the handleFiles .otk branch in `TakeoffCanvas.jsx`; `web/test/projectArchive.test.ts` |
| **Estimator profile (.otprofile)** | The working environment as one portable file (#299) — condition templates, material library, stamps, report templates/theme/columns; exactly the browser-global records and never project data, so applying a profile can't touch a takeoff. **Sheet → Export/Import/Reset profile** — import and reset download the current profile first (the receipt is the rollback); every section rides its normal sanitize gate; unknown schemas refuse loudly | `web/src/lib/profile.js`; profile functions + Sheet-menu Profile section in `TakeoffCanvas.jsx`; `web/test/profile.test.ts` |
| **Manage plan set** | Gallery → **Manage** (#301): every stored PDF with page count, open tabs, and takeoff count — multi-select **Remove selected** (bytes go, takeoffs persist in annotations and restore on re-add) and local-mode **Clear workspace** (all PDFs + takeoff reset, non-empty takeoff snapshotted first so Revisions restores it). Page counts persist per project once discovered (#302), so a known set's gallery opens without reading a byte of it — the cache drops only on revision/removal; removing a PDF frees its pdf.js worker doc | manage mode in `web/src/components/PlanNavigator.jsx`; `closePdfs` / `clearWorkspace` / the `sheet_pages:` cache + `evictDoc` in `TakeoffCanvas.jsx` |
| **Measurement correctness** | Recalibration updates stored quantities and cutout restore snapshots; conflicting dimensional imports refuse atomically; legacy agent work stays pending; sync keeps each sheet's scale and geometry together | `shapeMetrics.js`, `importTakeoff.js`, `reviewState.js`, `sync/merge.js`; [design](docs/design/MEASUREMENT_CORRECTNESS.md), [verification](docs/design/measurement-correctness-verification.md) |
| **Sample plan** | One-click demo: a real (public) medical-center floor finish plan | `web/public/demo/`, load button in the empty state |
| **Bring-your-own-AI (opt-in)** | Client-side seam to a vision endpoint **you** configure (OpenAI-style or Anthropic-style; local runtimes welcome, key optional, stored per-browser). First consumer: *read scale with AI*—one title-block snapshot → suggestion in the existing confirm-to-apply flow. Dormant when unconfigured; no telemetry | `web/src/lib/ai.js` (`visionQuery`, config, request/response plumbing); `scaleFromLabel` in `web/src/lib/sheets.ts`; `captureTitleBlock`/`readScaleWithAi` in `TakeoffCanvas.jsx`; `web/src/components/AiSettings.jsx` |
| **Optional AI backend** | Pluggable adapter interface for scale/room/finish suggestions; heuristic default, bring your own model | `server/app.py`, `server/adapters/base.py`, `server/adapters/heuristic.py` |
| **Capture layer (opt-in)** | Local stdlib-only server banks Contribute payloads as (geometry → label) JSONL training rows—hash-gated dedup, verbatim payload archive, atomic mirror into a synced share. The mirror is wedge-proof: share ops run on capped expendable threads (`OPENTAKEOFF_MIRROR_TIMEOUT_S`, 15s default) behind a strand budget, so a stalled sync client can never hang a contribution | `capture/capture_server.py` (see `capture/README.md`); payload builder `web/src/lib/contribute.js` |
| **Proposals** | An agent's batch with one identity (#365): `propose_takeoff` opens it, every commit after attaches (`origin.proposal_id`), `revise_proposal` / `withdraw_proposal` replace or remove its pending shapes as one journal step, accepted shapes untouched. A condition edit is a **diff held pending** (`propose_condition_edit`) until the estimator accepts it; totals and the report compute from the current values with the diff beside them. Canvas: one Accept / Reject pill per batch; the diff under its row in the Takeoffs panel; `proposed:` beside the row in the Report | `mcp/src/session.ts` (`stampProposal`, `proposeTakeoff`, `reviseProposal`, `withdrawProposal`, `proposeConditionEdit`, `acceptConditionEdit`), `web/src/lib/proposals.js` (`pendingByProposal`, `acceptConditionEditProposal`, `proposedConditionEditRows`); pills + accept/reject in `TakeoffCanvas.jsx`, the strip in `TakeoffsPanel.jsx`, the chip in `ReportPanel.jsx`; `docs/design/PROPOSALS.md` |
| **Scope collision** | Two conditions claiming the same floor as a number that has to read zero (#366): exact polygon intersection (JTS overlay) between committed floor shapes per sheet; `shared_floor_sf` = Σ areas − union, once per cell; cross-condition pairs are collisions, same-condition pairs are double traces; deducts and runs are not claims; unmeasurable shapes named, never zero. MCP: `takeoff_summary.shared_floor_sf`, `scope_duplicates` (read-only, with a `look` region), `scope_merge` (trim by exact difference or delete when near-total; one undo step; ink refused). Canvas: `⚠ N` badge on the row, pair list under the active row, **Look** frames the pair. Same threshold as the room eval's gate, pinned by test | `web/src/lib/scopeCollision.js` (`scopeCollisions`, `subtractWinner`, `collisionsByCondition`), `mcp/src/session.ts` (`scopeDuplicates`, `scopeMerge`), badge + list in `TakeoffsPanel.jsx`, `frameShapes` / `lookAtCollision` in `TakeoffCanvas.jsx`; `docs/design/SCOPE_COLLISION.md` |
| **In-canvas takeoff agent** | BYO-key tool-use loop inside the app: the agent reads sheet text and schedules, traces rooms from the wall faces it can see (it aims the One-Click engine when the gate is lifted), and stages **dashed proposals**—nothing commits until the human Accept gate; refuses uncalibrated sheets (the scale gate) and whitelists proposal evidence | `web/src/lib/agentTools.js` (tool registry + `agentScaleGate` + `pickAgentEvidence`), `web/src/lib/agentLoop.js`, `AgentPanel.jsx`; `buildAgentCtx` + the accept gate in `TakeoffCanvas.jsx` |
| **Command box (voice wiring)** | **Gated off the toolbar by default** (`VITE_COMMAND_BOX=1` builds it back; grammar, recognizer and tests unchanged). Toolbar command line: `carpet one waste 7` / `waste 12` / `label Phase 2` / `clear label` / `note …`—a deterministic transcript→intent grammar dispatched through the exact capability calls the buttons make (no voice-only mutation path, proven by state-equivalence tests); the entry point RFC #59's push-to-talk dictation will feed | parser `web/src/lib/voiceIntent.ts`; dispatcher `web/src/lib/voiceActions.ts`; `buildVoiceCtx` + the Command cluster in `TakeoffCanvas.jsx` |
| **Import takeoff** | Sheet menu → **Import takeoff…** loads a `takeoff_canvas.v1` JSON—the app's own autosave or an MCP session's `export_takeoff`—through the review gate: machine shapes (`origin.reviewed:false`) land dashed in their condition colors until the Accept banner inks them. Merge rules are pure + tested: operator's calibration, conditions, and workspace win; re-import is idempotent | `web/src/lib/importTakeoff.js` (`parseTakeoffImport`, `mergeTakeoffImport`); hidden input + `importTakeoffFile` + Sheet-menu entry in `TakeoffCanvas.jsx`; `web/test/importTakeoff.test.ts` |
| **MCP server** | The engine on stdio for MCP clients (45 tools on a default build — `one_click` and `detect_rooms` are temporarily gated and not registered; `OPENTAKEOFF_ONE_CLICK=1` registers them, 47): load/scale/one-click/**detect-rooms** (batch one-click from the sheet's own room-number labels; **assign-from-schedule** commits each room under its own schedule row's FLOOR finish, withholding what the schedule can't answer)/measure/summary/export as agent tools, **edit_condition** (the waste % + ×N multiplier knobs—without them an agent takeoff ships net === gross), **export_report** (the computed `opentakeoff.report.v1` document—the buy list and waste-adjusted nets a pricing consumer reads instead of re-deriving), **measure_surface** + **place_count** (wall SF and EA—all five measure roles reach the wire, #146), **roll_setup on edit_condition** (roll goods figured headlessly, the reply echoes the order, #147), **list_shapes** (the mid-session inventory, #149), **room labels on the wire** (`detect_rooms` stamps each committed room with the number it traced from and `edit_shape` takes a `label`—the per-shape field the Report and the floor × room tab group by), **`seam_lf` as an `edit_materials` basis** (weld rod / seam tape off the figured roll layout, 0 without a `roll_setup` rather than a guess), **annotate incl. arrow/bubble/dimension** (#150), **mark_verdict + delete_verdict** (the agent's AGENT-diamond verdict on its own work—the approval family's agent half, no actor input so the estimator's seal stays human-only, marked-set cover tallies the split, #176 companion), **symbol_sweep** (every instance of a repeated symbol from one marqueed example—deterministic linework matching under rotation/mirror, near-misses withheld with reasons, matches committable as EA in one undo step; `scope: "set"` sweeps the whole set counting on plan-role sheets only, seedable from a detail/legend sheet; counter-examples via `exclude`—rects around the lookalikes you do NOT mean, every rejection disclosed in `rejected[]` and reinstatable, #259; `luminance_tolerance` gates on per-segment stroke luminance for flattened exports, opt-in and disclosed in `lum_gate`, #260), **sweep_schedule_row** (mint a condition from a schedule row and count its drawn markers across the plan sheets—geometry and tag text agreeing, unanchorable rows refused with reasons), **derive_base** (base LF = perimeter − stated openings, #148), **derive_transitions** (the transition where two finishes meet—butt joints commit, and adjacency across a *wall* is withheld as a question with its length, gap and a point to look at, because the threshold is in a doorway the trace record cannot locate, #202), **condition twins** (`duplicate_condition` mints the same finish measured somewhere else—over a slab and over a raised deck take the same field material and different preparation underneath—carrying the original's materials and *following* them until a row is touched, and `split_condition` cuts the inheritance while keeping the tag and the sibling grouping, #204/#205), **import_takeoff** (resume/extend/audit through the app's merge rules, #151; correction rules ride the file too), **apply_rules** (re-run the correction rules an estimator taught the canvas—same pure `rules.ts` engine as Preview, committed as the one batch Apply makes, reviewed:false, idempotent by construction, #207), **cut_out** (a real hole in a committed floor shape through the canvas's own `cutout.js` boolean subtract—composes, never double-deducts, refuses an edge-crossing ring; undo and delete revert parent and hole together, #206), **multi-document sessions** (`load_plan merge`—plans + schedule + addenda as one set, #152), **mixed-scale warnings** (#153), **export_marked_pdf** (the marked-up planset—shapes, hatches, quantity chips, and annotations burned into vector copies of the sheets plus a legend cover, machine-traced work disclosed as pending review; the deliverable, stated in the server's initialize `instructions`), plus **view-sheet** sheet vision (rendered crops with a calibrated 1-ft/5-ft measuring grid, a committed-shapes overlay, and **disclosure marks**, #297 — question/struck/ring coordinate lists burned in off-CAD-pen colors, so what a sweep reply names the picture shows; `commit_seed` on symbol_sweep mints the sheet-scope seed into the same batch and the note flags it whenever left out, #296), same math and provenance as the canvas | `mcp/server.ts`, `mcp/src/{session,tools,pdf,view,marked}.ts`; batch core in `web/src/lib/detectRooms.ts`; report document from `web/src/lib/totals.js` `reportJson`; marked set from `web/src/lib/markedset.js` (see `mcp/README.md`, `docs/MCP.md`) |
| **Staged tool exposure (opt-in)** | Behind `OPENTAKEOFF_MCP_STAGED_TOOLS=1`, the stdio server stages its 47 tools along the workflow the initialize instructions already teach instead of handing every client the flat contract: **setup** (11—orient: load, scale, read the set) starts enabled plus one opener, `open_tool_stage`, which enables the **measure** (11), **revise** (13), or **handoff** (6) group on demand and fires `tools/list_changed`. Opening is idempotent and never closes anything—the surface only grows; the flag unset is byte-identical to before, so no published client changes behavior. A test holds the stage table as an exact partition of the registered set, so a new tool can't land stageless (#230) | `mcp/src/staging.ts` (stage table + opener); wiring in `mcp/server.ts` (env flag + instructions line); `registerTools` returns the `RegisteredTool` map in `mcp/src/tools.ts`; test `mcp/test/staging.test.ts` |
| **Export overwrite guard** | The MCP export tools won't destroy a file they didn't write. The `path` argument stays unconstrained—the marked set belongs in the job folder, not the working directory—so the line is authorship, not location: re-exporting over a **previous export of our own** overwrites silently (the constant loop: fix a condition, export again, same path), while any other existing file is refused until the caller passes `overwrite: true`. Recognition is cheap and exact—both JSON exports stamp `schema` as their first key, so a 4KB head read settles it, and a marked set is identified by its PDF `Producer` (`markedset.js` now stamps `Producer`/`Creator` = `OpenTakeoff`, which doubles as provenance on a document that gets emailed around). Corrupt, encrypted, or unreadable counts as *not ours*, so it fails **closed**, and the check runs before the render so a refused write costs no build. Data-loss protection, explicitly not a sandbox—`SECURITY.md` states the threat model rather than implying one | `mcp/src/safewrite.ts` (`assertWritable`); wired in `mcp/src/tools.ts` (`export_takeoff`, `export_report`) and `mcp/src/marked.ts`; Producer stamp in `web/src/lib/markedset.js`; `mcp/test/safewrite.test.ts` |

## Tested surface

`cd web && npm test`—`node:test` over the pure math: `web/test/geometry.test.ts` (One-Click pipeline incl. the hatch-robust fill and meta emission), `web/test/canvas-geometry.test.ts` (angle lock, hit-testing, metrics, snap grid, highlighter stroke geometry), `web/test/totals.test.ts` (waste, SY, coverage → order quantities, vertical-wall SF), `web/test/coverage.test.ts` (material-kind classification, grout coverage from tile geometry), `web/test/units.test.ts` (metric display layer + ratio-scale math), `web/test/xlsx.test.ts` (the SpreadsheetML writer: escaping, sheet-name rules, styles/filters, workbook tabs incl. the floor × room cross-section, fflate round-trip), `web/test/seamLf.test.ts` (the figured seam rule: one seam per 20-ft room off a 12-ft roll, none for the same area as two 10-ft rooms, partial overlap on an L, and 0 without a layout), `web/test/revisions.test.ts` (revision diff: pairing, display-precision thresholds, sheet/buy-list deltas, compare CSV), `web/test/zone.test.ts` (zone-check classification + rollup), `web/test/plays.test.ts` (condition plays), and `web/test/variants.test.ts` (condition twins: following, per-row override, tombstones, re-base, split, promote-on-delete), `web/test/projectArchive.test.ts` (.otk build→parse round-trip incl. byte-verbatim plans, refusal of manifest-less zips / unknown schemas / hollow manifests), and `web/test/profile.test.ts` (the .otprofile parse gate).

`cd mcp && npm test`—the MCP server over the bundled demo plan: session state + lazy caches (`mcp/test/session.test.ts`), the tool layer over an in-memory client/server pair (`mcp/test/tools.test.ts`), and a full load → scale → one-click ×4 → summary → export round-trip (`mcp/test/e2e.test.ts`).

`python3 capture/capture_server.py selftest`—end-to-end over the wire: contribution rows, dedup on re-contribution, re-capture on retag, health counts, the atomic mirror copy, and the wedged-share guarantees (a hanging mirror folder still banks the row and returns the POST within the cap).

## Extending it

Start with [`AGENTS.md`](AGENTS.md) for the canvas mental model and conventions (coordinate spaces, imperative cursor layer, SVG color literals), then pick your row above. Typical forks: another trade's conditions and supporting materials, a new export format in `ReportPanel.jsx`, or a real model behind `server/adapters/base.py`.
